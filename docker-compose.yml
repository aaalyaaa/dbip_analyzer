version: '3.8'

services:
  dbip-analyzer:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data-raw:/app/data-raw
      - ./processed:/app/processed
      - ./docs:/app/docs
    working_dir: /app